import { url } from './global';

/*—— environmentState ————————*/
export interface environment {
  environmentId: number,
  environmentName: string,
  environmentIcon: url,
  environmentDiscription: string,
}
